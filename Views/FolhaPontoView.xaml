<Window x:Class="RegistroPonto.Views.FolhaPontoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folha de Ponto" Height="400" Width="600">
    <Grid>
        <TextBlock Text="Folha de Ponto" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20" FontSize="18" />
        <ListView Name="pontoListView" HorizontalAlignment="Center" VerticalAlignment="Center" Width="500" Height="250">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Data" DisplayMemberBinding="{Binding Data}" Width="150"/>
                    <GridViewColumn Header="Hora de Chegada" DisplayMemberBinding="{Binding HoraChegada}" Width="150"/>
                    <GridViewColumn Header="Hora de Saída" DisplayMemberBinding="{Binding HoraSaida}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Fechar" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20" Width="100" Command="{Binding CloseFolhaPontoCommand}" />
    </Grid>
</Window>
